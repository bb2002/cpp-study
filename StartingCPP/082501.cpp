#include "pch.h"
#include <iostream>
#include <vector>

using ll = long;
using IntVector = std::vector<int>;

int main() {
	IntVector newVector = IntVector();
	newVector.push_back(10);
	newVector.push_back(10);
	newVector.push_back(10);
	newVector.push_back(10);
	newVector.push_back(10);
	newVector.push_back(10);
}